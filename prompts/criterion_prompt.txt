Given the image, target name, refer object name, and detected refer-object box,
describe the visual relationship that can verify whether a candidate target mask is correct.

Return strict JSON only:
{
  "relationship": "brief relation between target and refer object",
  "positive_criteria": ["what should be true for a correct target mask"],
  "negative_criteria": ["what would indicate a wrong mask"]
}

Rules:
- Focus on visible spatial, functional, or attribute relations.
- Keep each criterion short and checkable.
- Do not include markdown or explanations outside JSON.
